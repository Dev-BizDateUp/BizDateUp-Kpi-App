1. learned the reduce method in js 
2. learned that we can share data between routes in frontend using state that react router provides
3. learned the newMap method in js 
4. study this     const uniqueEntries = Array.from(
      new Map(get_weekly_entries.map((item) => [item.kpis.id, item])).values()
    );